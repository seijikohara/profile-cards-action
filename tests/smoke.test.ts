import { describe, expect, it } from 'vitest';

// Placeholder until real unit tests land. `vitest run` fails when no test files
// exist, so this keeps the test step green for the skeleton.
describe('smoke', () => {
  it('runs the test suite', () => {
    expect(true).toBe(true);
  });
});
